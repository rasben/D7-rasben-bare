----------// Theme Information

  name        = Rasben Bare
  description = A bare theme, based on mothership by mortendk.
  core        = 7.x 
  base theme  = mothership
  engine      = phptemplate
  screenshot  = screenshot.png

scripts[] = js/build/base.min.js

plugins[panels][layouts] = layouts

; Theme
stylesheets[all][] = css/mobile.css

regions[top] = Top 
regions[header] = Header
regions[content] = Content
regions[footer] = Footer
